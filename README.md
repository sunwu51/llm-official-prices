# LLM Official Prices

Daily filtered snapshot of [models.dev](https://models.dev/api.json), containing recent models from selected first-party providers.

```sh
npm run generate
```

Prices are copied from models.dev in USD per one million tokens. Model IDs are lowercase; IDs containing `/` and models released more than six months ago are excluded.
